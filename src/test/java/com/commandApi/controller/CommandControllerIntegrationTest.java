package com.commandApi.controller;

/*
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.MockMvc;
import org.springframework.test.web.servlet.request.MockMvcRequestBuilders;
import org.springframework.test.web.servlet.result.MockMvcResultMatchers;

@SpringBootTest
@AutoConfigureMockMvc
public class CommandControllerIntegrationTest {

    @Autowired
    private MockMvc mockMvc;

    @Test
    public void testAssociateCommandToSpecificClients() throws Exception {
        String commandId = "1";
        String clientIds = "1,2,3";

        mockMvc.perform(MockMvcRequestBuilders.put("/api/v1/command/orderId/{id}/clientIds/{clientTds}", commandId, clientIds)
                        .contentType(MediaType.APPLICATION_JSON))
                .andExpect(MockMvcResultMatchers.status().isOk())
                .andExpect(MockMvcResultMatchers.content().string(org.hamcrest.Matchers.containsString("A client that you set does not exist")));

    }

    @Test
    public void testAssociateProductsToTheCommand() throws Exception {
        String commandId = "1";
        String productIds = "1,2,3";

        mockMvc.perform(MockMvcRequestBuilders.put("/api/v1/command/orderId/{id}/productIds/{productsTds}", commandId, productIds)
                        .contentType(MediaType.APPLICATION_JSON))
                .andExpect(MockMvcResultMatchers.status().isOk())
                .andExpect(MockMvcResultMatchers.content().string(org.hamcrest.Matchers.containsString("A product that you set does not exist")));

    }
}*/
